# Icons

Place your app icon PNG files here. These are displayed in the SwiftDialog progress window during onboarding.

Each app in `apps.conf` references an icon filename (e.g., `app-01.png`). Add a PNG for each app and a `logo.png` for the organization logo used in the dialog header and completion screen.
